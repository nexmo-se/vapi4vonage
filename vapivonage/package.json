{
  "name": "vapivonage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "export SET NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service serve",
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service build",
    "dev": "export SET NODE_OPTIONS=--openssl-legacy-provider && vue-cli-service serve"
  },
  "dependencies": {
    "@vonagevolta/vue": "^0.2.1",
    "axios": "^0.27.2",
    "nexmo-client": "^8.7.3",
    "vue": "^2.6.11",
    "vue-axios": "^3.4.1"
  },
  "devDependencies": {
    "@vue/cli-service": "~4.4.6",
    "vue-template-compiler": "^2.6.11"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
